C:\windows\system32\cmd.exe /c (python byl_autopy.py)
proc_ws will start

proc_bg will start

process write_stop runing....

process begin_begin runing

begin_exit

jingru_byl

subProcess_shouyou run

open_game will run

open_game

if_stop will run

bg put begin

bg que_stop.get(True) == True
time.sleep(3)
open_byl will run

open_byl

if_stop will run

bg put begin

ws diff--> True

#----------------------------
C:\windows\system32\cmd.exe /c (python byl_autopy.py)
proc_ws will start

proc_bg will start

process write_stop runing....

process begin_begin runing

begin_exit

jingru_byl

subProcess_shouyou run

open_game will run

ws diff--> True

ws contine
open_game

if_stop will run

bg que_stop.get(True) == True
bg will exit, put continue

twime.sleep(3)s diff--> True


ws contine
open_byl will run

ws diff--> False

ws contine
open_byl

ws diff--> True

ws contine
ws diff--> False

ws contine
ws diff--> False

ws contine
ws diff--> False

ws contine
ws diff--> True

ws contine
ws diff--> True

ws contine
ws diff--> True

ws contine
ws diff--> False

ws contine
ws diff--> False

ws contine
ws diff--> True

ws contine
ws diff--> True

ws contine
ws diff--> True

ws contine
ws diff--> True

ws contine
ws diff--> True

ws contine
ws diff--> False

ws contine
ws diff--> False

ws contine
ws diff--> False

ws contine
ws diff--> False

ws contine
ws diff--> True

ws contine
if_stop will run

bg que_stop.get(True) == True
bg will exit, put continue

close_gongGao will run

close_gongGao

ws diff--> True

ws contine
ws diff--> False

ws contine
ws diff--> False

ws contine
ws diff--> False

ws contine
ws diff--> False

ws contine
ws diff--> True

ws contine
if_stop will run

bg que_stop.get(True) is False
bg will exit, put exit

exit_shouyou
成功: 已终止进程 "AppMarket.exe"，其 PID 为 2160。
成功: 已终止进程 "AndroidEmulator.exe"，其 PID 为 5040。
成功: 已终止进程 "adb.exe"，其 PID 为 4820。
bg que_stop.get(True) == True
bg will exit, put continue

close_zhiDaole

if_stop will run

close_gongGao will run

bg que_stop.get(True) == True
bg will exit, put continue

ws diff--> True

ws contine
ws diff--> False

ws contine
open_beiLvMs

ws diff--> False

ws contine
open_room

ws diff--> False

ws contine
