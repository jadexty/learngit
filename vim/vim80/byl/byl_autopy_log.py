
C:\windows\system32\cmd.exe /c (python byl_autopy.py)
proc_ws will start

proc_bg will start

write_stop runing....

begin_begin runing

begin_exit

jingru_byl

subProcess_shouyou run

open_game will run

diff--> True

open_game

if_stop will run

que_stop.get(True) not True

time.sleep(3)
diff--> True

open_byl will run

open_byl

que_stop.get(True) not True

diff--> True

diff--> False

diff--> False

diff--> False

diff--> False

diff--> True

diff--> True

diff--> False

diff--> False

diff--> False

diff--> True

diff--> True

diff--> True

diff--> True

diff--> False

diff--> False

diff--> False

diff--> False

diff--> False

close_gongGao will run

que_stop.get(True) not True

close_gongGao

time.sleep(20)
diff--> True

diff--> True

diff--> False

diff--> False

diff--> False

diff--> True

close_zhiDaole

diff--> True

open_beiLvMs

diff--> True

diff--> True

open_room

diff--> True

diff--> False

diff--> False

diff--> True

diff--> True

diff--> True

diff--> True

diff--> True

exit_shouyou
成功: 已终止进程 "AppMarket.exe"，其 PID 为 1672。
成功: 已终止进程 "AndroidEmulator.exe"，其 PID 为 4700。
成功: 已终止进程 "adb.exe"，其 PID 为 532。
diff--> True

diff--> False

begin_exit

jingru_byl

subProcess_shouyou run

diff--> False

open_game will run

open_game

if_stop will run

que_stop.get(True) is True

exit_shouyou
成功: 已终止进程 "AppMarket.exe"，其 PID 为 3140。
错误: 没有找到进程 "AndroidEmulator.exe"。
错误: 没有找到进程 "adb.exe"。
que_stop.get(True) not True

time.sleep(3)
diff--> True

open_byl will run

open_byl

que_stop.get(True) not True

diff--> False

Process Process-1:
Traceback (most recent call last):
  File "C:\Python27\lib\multiprocessing\process.py", line 258, in _bootstrap
    self.run()
  File "C:\Python27\lib\multiprocessing\process.py", line 114, in run
    self._target(*self._args, **self._kwargs)
  File "C:\vim\vim80\byl\byl_autopy.py", line 40, in write_stop
    col = autopy.bitmap.capture_screen().get_color(pos[0], pos[1])
ValueError: Point out of bounds
#----------------------------
proc_ws will start

proc_bg will start

write_stop runing....
begin_begin runing

begin_exit

jingru_byl

subProcess_shouyou run

open_game will run

diff--> True

请按任意键继续. . . open_game

if_stop will run

que_stop.get(True) not True

